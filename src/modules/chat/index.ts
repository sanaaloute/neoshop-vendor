/**
 * Vendor messaging — split inbox, attachments, realtime (Socket.IO).
 * @module modules/chat
 */

export { MessagingHome } from "./messaging-home";
export type {
  ChatThread,
  ChatMessage,
  ChatAttachment,
  ChatAuthorRole,
  ChatParticipant,
} from "./types";
